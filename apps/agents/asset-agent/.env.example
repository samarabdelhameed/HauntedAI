# ✨ Pollination AI - FREE Image Generation (No API Key Needed!)
# OPENAI_API_KEY is now OPTIONAL - we use Pollination AI by default
# OPENAI_API_KEY=your_openai_api_key_here

# Image Generation Provider
IMAGE_PROVIDER=pollination  # Options: pollination (free), openai (requires key)

# Server Configuration
PORT=3003

# Storacha Configuration (optional - uses default if not set)
# STORACHA_DID=your_storacha_did_here
