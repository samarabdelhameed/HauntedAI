// Generated by Kiro - CodeAgent Entry Point
import dotenv from 'dotenv';

// Load environment variables FIRST before any other imports
dotenv.config();

import { CodeAgentServer } from './server';

// Validate required environment variables
if (!process.env.HUGGINGFACE_API_KEY) {
  console.error('ERROR: HUGGINGFACE_API_KEY environment variable is required');
  console.error('Get your FREE token from: https://huggingface.co/settings/tokens');
  process.exit(1);
}

// Start the server
const server = new CodeAgentServer();
server.start();
