// Generated by Kiro
// Type definitions for agent logs and SSE events
// Requirements: 5.1, 5.2

/**
 * Agent log message structure
 * Used for real-time log streaming via SSE
 */
export interface AgentLog {
  timestamp: Date;
  agentType: 'story' | 'asset' | 'code' | 'deploy' | 'orchestrator';
  level: 'info' | 'warn' | 'error' | 'success';
  message: string;
  metadata?: Record<string, any>;
}

/**
 * SSE event types
 */
export type SSEEventType = 'connected' | 'log' | 'heartbeat' | 'error';

/**
 * SSE event payload
 */
export interface SSEEvent {
  event: SSEEventType;
  data: any;
}
