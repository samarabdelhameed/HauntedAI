# 🔧 حل المشاكل - HauntedAI

## ❌ خطأ: "ERR_CONNECTION_REFUSED" عند الضغط على "Connect Wallet"

### المشكلة:

عند الضغط على زر "Connect Wallet" في الصفحة الرئيسية، تظهر رسالة خطأ:

```
Failed to load resource: net::ERR_CONNECTION_REFUSED
Login failed: Failed to fetch
```

### السبب:

الخادم الخلفي (Backend API) غير متاح على `http://localhost:3001`

### الحل:

#### الخيار 1: تشغيل API (موصى به)

```bash
# في terminal منفصل
npm run dev:api
```

أو:

```bash
cd apps/api
npm run dev
```

بعد تشغيل API، جرب ربط المحفظة مرة أخرى.

---

#### الخيار 2: المتابعة بدون API (للاستكشاف فقط)

عند ظهور رسالة الخطأ، سيظهر لك خيار:

- **"هل تريد المتابعة بدون API؟"**

اضغط **"موافق"** للاستمرار في استكشاف الواجهة بدون API.

**ملاحظة:** بعض الميزات لن تعمل بدون API (مثل إنشاء غرف حقيقية، الحصول على مكافآت).

---

## ⚠️ خطأ: "React is running in production mode"

### المشكلة:

```
Uncaught Error: React is running in production mode, but dead code elimination has not been applied.
```

### الحل:

تم إصلاح هذا الخطأ في `vite.config.ts`. إذا استمر، قم بـ:

```bash
# إعادة تشغيل dev server
# اضغط Ctrl+C لإيقاف الخادم
npm run dev:web
```

---

## 🔍 خطأ: "Could not establish connection" (Polkadot.js)

### المشكلة:

```
Attempt 1 failed: Could not establish connection. Receiving end does not exist.
Disconnected from polkadot{.js}
```

### السبب:

هذا خطأ من إضافة Polkadot.js في المتصفح (ليس من المشروع).

### الحل:

هذا الخطأ **غير مؤثر** ويمكن تجاهله. لا يؤثر على عمل المشروع.

---

## ✅ التحقق من أن كل شيء يعمل

### 1. Frontend يعمل:

```bash
npm run dev:web
```

افتح: http://localhost:5173

### 2. Backend API يعمل:

```bash
npm run dev:api
```

افتح: http://localhost:3001/health

### 3. اختبار الاتصال:

```bash
npm run test:journey
```

---

## 📋 خطوات سريعة لحل مشكلة "Connect Wallet"

```
1. افتح terminal جديد
   ↓
2. شغّل: npm run dev:api
   ↓
3. انتظر حتى ترى: "Nest application successfully started"
   ↓
4. ارجع للمتصفح واضغط "Connect Wallet" مرة أخرى
   ↓
5. يجب أن يعمل الآن! ✅
```

---

## 🆘 إذا استمرت المشكلة

1. **تأكد من المنافذ:**
   - Frontend: `5173`
   - API: `3001`

2. **تحقق من المتغيرات البيئية:**

   ```bash
   # في apps/web/.env
   VITE_API_URL=http://localhost:3001
   ```

3. **أعد تشغيل الخوادم:**
   ```bash
   # أوقف جميع الخوادم (Ctrl+C)
   # ثم أعد تشغيلها
   npm run dev:web
   npm run dev:api
   ```

---

**🎃 إذا واجهت أي مشكلة أخرى، راجع ملف `USER_GUIDE_AR.md`**
